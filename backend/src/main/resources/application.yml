server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: collab-docs
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3307/collab_docs?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${MYSQL_USER:collab}
    password: ${MYSQL_PASSWORD:collab_dev_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:collab}
    password: ${RABBITMQ_PASSWORD:collab_dev_password}
    listener:
      simple:
        acknowledge-mode: manual
  elasticsearch:
    uris: ${ELASTICSEARCH_URIS:http://localhost:9200}
  jackson:
    default-property-inclusion: always
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

collab:
  jwt:
    # 仅本地开发默认值；部署时必须通过环境变量覆盖，并至少 32 字节。
    secret: ${JWT_SECRET:change-this-local-development-secret-key-please}
    expire-hours: ${JWT_EXPIRE_HOURS:24}
  minio:
    endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
    public-endpoint: ${MINIO_PUBLIC_ENDPOINT:http://localhost:9000}
    access-key: ${MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${MINIO_SECRET_KEY:minioadmin}
    bucket: ${MINIO_BUCKET:collab}

logging:
  level:
    com.school.collab: info
